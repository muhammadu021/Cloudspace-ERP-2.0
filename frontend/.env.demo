# ClearDesk DEMO MODE Environment
# This file configures the frontend to run in fully offline demo mode
# with zero backend/API calls and all data stored locally in the browser

# Enable Demo Mode - Set to 'true' to activate offline demo functionality
VITE_DEMO_MODE=true

# API URL (not used in demo mode, but kept for fallback)
# In demo mode, all requests are intercepted and served from localStorage
VITE_API_URL=http://localhost:5050/cleardesk/api/v1

# Environment
VITE_ENV=demo

# Optional: Verbose logging for demo adapter and data layer
VITE_DEMO_VERBOSE=true
